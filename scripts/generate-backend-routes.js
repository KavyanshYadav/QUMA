// eslint-disable-next-line @nx/enforce-module-boundaries
import { AppRouter } from '../packages/quma_config/dist/index.js';
console.log(AppRouter);

for (const key in AppRouter) {
  for (const key2 in AppRouter[key]) {
  }
}
