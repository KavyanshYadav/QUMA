FROM node:20-slim

WORKDIR /app

COPY package*.json pnpm-lock.yaml* ./

RUN npm install -g pnpm && pnpm install


COPY . . 

ENV NODE_ENV=development
ENV PORT=7011
ENV CONSUL_HOST=consul
ENV CONSUL_PORT=8500


EXPOSE 7011

RUN pnpm install -D nodemon

CMD ["pnpm", "run", "dev"]
